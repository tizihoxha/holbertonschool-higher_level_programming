#!/usr/bin/python3
"""Task 2"""


def is_same_class(obj, a_class):
    """True if the object is an instance of the specified class else False"""
    if type(obj) == a_class:
        return (true)
    else:
        return (false)
