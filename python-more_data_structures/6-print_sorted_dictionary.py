#!/usr/bin/python3
def print_sorted_dictionary(a_dictionary):
    sorted(a_dictionary) = a_dictionary
    keys = [list(a_dictionary.keys())]
    values = [list(a_dictioary.values())]
    print("{}: {}".format(keys, values))
